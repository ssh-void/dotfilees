#!/usr/bin/env bash

# Enable root mode on the Android device (if supported)
adb root

# Copy the local hosts file to the user's home directory
cp /etc/hosts ~/hosts

# Push the hosts file to the Android device
adb push ~/hosts /etc/hosts

# Delete the temporary local hosts file
rm -rf ~/hosts
