#!/usr/bin/env bash

FILE=~/.config/.iptv.txt

selection=$(awk '{print $0}' "$FILE" | fzy -i -l 20 -p "iptv > ")

[ -z "$selection" ] && exit

url=$(echo "$selection" | cut -d'#' -f1 | awk '{print $1}')

if [ -n "$url" ]; then
   streamlink "$url" best --player vlc
fi
