#!/bin/bash 
# Update void-package
cd /opt/void-packages
# git clean -fd && git reset --hard && git pull (My files will be deleted)
git pull --autostash --rebase
./xbps-src show-sys-updates # update
#./xbps-src update-sys 
./xbps-src clean  # clean
./xbps-src clean-repocache
rm -rvf /opt/void-packages/hostdir/sources/*
rm -rvf /opt/void-packages/hostdir/binpkgs/*
